version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: crm_pxx
      POSTGRES_USER: pxx_admin
      POSTGRES_PASSWORD: pxx_secret_local
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
